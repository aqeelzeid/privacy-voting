// Jest test setup file
// This file runs before all tests

// Add any global test setup here
// For example, you could set up mocks for cryptographic functions

// Mock crypto.randomBytes if needed for deterministic testing
// Object.defineProperty(global, 'crypto', {
//   value: {
//     randomBytes: (size: number) => Buffer.alloc(size, 1),
//     // Add other crypto methods as needed
//   },
// });

// Export any test utilities
export {};
